# RTL Directory - Performance Comparison Demo

## Contents

Uses same RTL as `demo_fir_irq` (hardware unchanged).

Symlink or copy:
```bash
ln -s ../../demo_fir_irq/rtl/fir_q15_axil_irq.v .
ln -s ../../demo_fir_irq/rtl/fir_q15_core.v .
ln -s ../../demo_fir_irq/rtl/test_fir_irq.py .
ln -s ../../demo_fir_irq/rtl/Makefile .
```

No hardware changes needed for performance comparison (software-only difference).
